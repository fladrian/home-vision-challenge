import { AppLayout } from '@/components/layout/app-layout';
import { AppRoutes } from '@/routes/app-routes';

export const App = () => (
    <AppLayout>
      <AppRoutes />
    </AppLayout>
);

export default App;
